1. The bug was that num1 and num2 were retrieved as strings instead of numeric values. For this reasoning, JavaScript performed a string concatenation instead of addition. 
   
2. I would fix this bug by converting the values from string to numeric values using the Number() function.
 